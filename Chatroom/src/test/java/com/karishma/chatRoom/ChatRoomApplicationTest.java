package com.karishma.chatRoom;


//@RunWith(SpringRunner.class)
//@SpringBootTest
//@AutoConfigureMockMvc
public class ChatRoomApplicationTest {

	/*
	@Autowired
    private MockMvc mvc;
	
	@Test
	public void testGetChatroomListSuccess() throws Exception 
	{

        mvc.perform(MockMvcRequestBuilders.get("/rooms")).andExpect(status().isUnauthorized()).andReturn();
	}
	
	@Test
	public void testIncorrectUserCred() throws Exception {
	    String username = "testuser1";
	    String password = "test12345";

	    String body = "{\"username\":\"" + username + "\", \"password\":\" "
	                  + password + "\"}";

	    mvc.perform(MockMvcRequestBuilders.post("/login")
	    		.contentType(MediaType.APPLICATION_JSON_VALUE)
	            .content(body))
	            .andExpect(status().isUnauthorized()).andReturn();
	}
	
	*/
	
}
